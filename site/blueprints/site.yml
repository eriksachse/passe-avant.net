title: Site

tabs:
  content:
    label: Content
    icon: 📚
    sections:
      content_tab:
        type: pages
        status: listed
        headline: Subpages
        layout: cards
        templates:
          - reviews
          - features
          - opinions
          - interviews
        image:
          ratio: 3/2
          cover: true
        info: "{{ page.children.count }}"

  site:
    label: Everything else
    icon: 🔍
    sections:
      ee_tab:
        headline: Everything Else
        type: pages
        templates:
          - ads
          - about
          - blank
